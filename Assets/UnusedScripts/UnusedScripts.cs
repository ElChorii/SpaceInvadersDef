using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UnusedScripts : MonoBehaviour
{
    //Used to rotate the player when moving back adn forward  //PlayerBehaviour

    /*Quaternion originalRotationValues = gameObject.transform.rotation;
    LeanTween.rotateAround(gameObject, Vector3.up, 360, playerRotationTime).setEase(playerRotationAnim).setOnComplete(() =>
            {
        gameObject.transform.rotation = originalRotationValues;
    });*/
}
